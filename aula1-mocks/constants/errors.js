const errors = {
    invalidHeaders: 'Headers must be id, name, profession, age',
    invalidLength: 'Length must be more than one and less than three lines.',
    invalidFormat: 'All columns must have the same length.'
}

module.exports = { errors }